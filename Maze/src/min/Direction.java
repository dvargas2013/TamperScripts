package min;

/* 
 *  which driection that Snake is moving.
 */
public enum Direction {
	//Moving North (Up).
	North,
	//Moving East (Right).
	East,
	// Moving South (Down).
	South,
	//Moving West (Left).
	West
	
}
